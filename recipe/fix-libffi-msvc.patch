--- Cargo.toml.prev	1900-01-00 00:00:00 +0000
+++ Cargo.toml	1900-01-00 00:00:00 +0000
@@ -167,6 +167,9 @@
 winapi = "=0.3.9"
 windows-sys = { version = "0.48.0", features = ["Win32_Media"] }
 
+[patch.crates-io]
+libffi-sys = { git = "https://github.com/mdekstrand/libffi-rs", branch = "patch-msvc-build" }
+
 # NB: the `bench` and `release` profiles must remain EXACTLY the same.
 [profile.release]
 codegen-units = 1
